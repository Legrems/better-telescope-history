-- Plugin entry point for telescope-history
-- This file is automatically sourced by Neovim

-- Prevent loading the plugin twice
if vim.g.loaded_telescope_history then
  return
end
vim.g.loaded_telescope_history = true

-- The plugin will be initialized through the user's config
-- No automatic setup here to allow users to configure it first

